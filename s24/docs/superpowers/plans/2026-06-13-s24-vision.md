# S24 Vision Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Нативное Android-приложение для Samsung S24, которое распознаёт объекты камерой (YOLOE-26), пишет видео с рамками, обучает/дообучает профили людей (лицо+тело) и объектов (эмбеддинги) по роликам, и показывает нагрузку CPU/GPU.

**Architecture:** Kotlin + Jetpack Compose (UI) + CameraX (камеры) + ONNX Runtime Mobile (все модели, заморожены). Единая парадигма распознавания: эмбеддинг → косинусная близость к профилям. Модели готовятся один раз на Mac скриптом-экспортёром и кладутся в `assets/models/`. Всё на устройстве, без облака и Python на телефоне.

**Tech Stack:** Kotlin, Jetpack Compose, CameraX, ONNX Runtime Android (`com.microsoft.onnxruntime:onnxruntime-android`), Media3/MediaCodec, Ultralytics (только на Mac для экспорта), Python venv от `hikvision-test1` (для экспорта).

---

## Опорные факты (проверено)

- ONNX Runtime Android Kotlin API: `OrtEnvironment.getEnvironment()`, `OrtSession.SessionOptions().apply { addNnapi(EnumSet.of(NNAPIFlags.USE_FP16)) }`, `env.createSession(modelBytes, options)`, `OnnxTensor.createTensor(env, FloatBuffer, longArrayOf(...))`, `session.run(mapOf(name to tensor))`. Зависимость: `implementation("com.microsoft.onnxruntime:onnxruntime-android:1.17.0")`.
- YOLOE экспорт: visual-prompt **запекается при экспорте**, ONNX не принимает новые промпты на устройстве. Поэтому используем **prompt-free** вариант для локализации + MobileCLIP-эмбеддинг кропа для узнавания обученных объектов.
- YOLO26/YOLOE экспорт: `YOLO("yolo26n.pt").export(format="onnx", opset=...)`.
- У пользователя уже есть `yolo26n.pt` и venv в `/Users/yuragms/Cursor-cash/hikvision-test1`.

## File Structure

Экспорт моделей (на Mac):
- Create: `s24/tools/export_models.py` — экспортирует все ONNX в `s24/app/src/main/assets/models/`.
- Create: `s24/tools/README.md` — как запустить экспорт в venv.

Android-проект `s24/app/` (пакет `com.s24vision.app`):
- Gradle: `s24/settings.gradle.kts`, `s24/build.gradle.kts`, `s24/app/build.gradle.kts`, `s24/gradle.properties`, `s24/app/src/main/AndroidManifest.xml`.
- `core/math/Embeddings.kt` — l2-нормализация, косинус, агрегация (чистые функции, тестируемо).
- `core/profiles/Profile.kt` — модель профиля (тип, имя, эмбеддинги).
- `core/profiles/ProfileStore.kt` — чтение/запись/слияние `.bin` + `meta.json`, сообщения «создан/улучшен».
- `core/monitor/CpuMonitor.kt` — парсинг `/proc/stat` + `/proc/self/stat` → проценты.
- `core/monitor/GpuMonitor.kt` — best-effort Adreno sysfs → проценты или `n/a`.
- `core/onnx/OnnxModel.kt` — обёртка над OrtSession (load из assets, run).
- `vision/Detection.kt` — data-класс бокса (label, conf, rect).
- `vision/YoloeDetector.kt` — препроцесс, инференс, постобработка боксов.
- `vision/FaceRecognizer.kt` — SCRFD + выравнивание + ArcFace embedding.
- `vision/BodyRecognizer.kt` — OSNet embedding кропа.
- `vision/ObjectRecognizer.kt` — MobileCLIP embedding кропа + матч с профилями.
- `vision/IdentityFusion.kt` — формула `total` (порт из hikvision).
- `vision/FramePipeline.kt` — связывает детектор+распознаватели → список аннотаций кадра.
- `enroll/Enroller.kt` — сэмплинг кадров видео, сбор эмбеддингов, создание/дообучение профиля.
- `record/FrameRecorder.kt` — запись аннотированных кадров в mp4.
- `camera/CameraController.kt` — CameraX, переключение селфи/основная, поток кадров.
- `ui/` — Compose: `CameraScreen.kt`, `RecordingsScreen.kt`, `TrainScreen.kt`, `ProfilesScreen.kt`, `Nav.kt`, `MainActivity.kt`, `OverlayView.kt`.
- Тесты (JVM unit, `app/src/test/...`): `EmbeddingsTest.kt`, `ProfileStoreTest.kt`, `CpuMonitorParseTest.kt`, `IdentityFusionTest.kt`.

---

## Phase 0 — Подготовка моделей (Mac)

### Task 1: Скрипт экспорта моделей в ONNX

**Files:**
- Create: `s24/tools/export_models.py`
- Create: `s24/tools/README.md`

- [ ] **Step 1: Написать скрипт экспорта**

```python
# s24/tools/export_models.py
"""Экспорт всех ONNX-моделей в app/src/main/assets/models/.
Запуск из venv hikvision-test1 (где установлены ultralytics, insightface, torchreid)."""
import shutil
from pathlib import Path
from ultralytics import YOLO

ROOT = Path(__file__).resolve().parents[1]
OUT = ROOT / "app/src/main/assets/models"
OUT.mkdir(parents=True, exist_ok=True)

def export_yoloe():
    # prompt-free YOLOE-26 small; имя файла актуализировать по релизу пакета
    model = YOLO("yoloe-26s.pt")
    path = model.export(format="onnx", opset=17, dynamic=False, simplify=True)
    shutil.copy(path, OUT / "yoloe26s.onnx")

def export_mobileclip():
    # image encoder MobileCLIP-S0 -> onnx (см. README, экспортируется отдельным скриптом open_clip/mobileclip)
    src = Path("mobileclip_s0_image.onnx")
    if src.exists():
        shutil.copy(src, OUT / "object_encoder.onnx")
    else:
        print("WARN: mobileclip_s0_image.onnx не найден — экспортируйте отдельно (см. README)")

def copy_insightface():
    base = Path.home() / ".insightface/models/buffalo_sc"
    shutil.copy(base / "det_500m.onnx", OUT / "face_det.onnx")
    shutil.copy(base / "w600k_mbf.onnx", OUT / "face_rec.onnx")

def export_osnet():
    import torch, torchreid
    m = torchreid.models.build_model("osnet_x0_25", num_classes=1000, pretrained=True)
    m.eval()
    dummy = torch.randn(1, 3, 256, 128)
    torch.onnx.export(m, dummy, str(OUT / "body_reid.onnx"),
                      input_names=["input"], output_names=["embedding"], opset_version=17)

if __name__ == "__main__":
    export_yoloe()
    copy_insightface()
    export_osnet()
    export_mobileclip()
    print("Done ->", OUT)
```

- [ ] **Step 2: Запустить экспорт в venv и проверить файлы**

Run:
```bash
cd /Users/yuragms/Cursor-cash/hikvision-test1
source .venv/bin/activate
pip install -U ultralytics
python /Users/yuragms/Cursor-cash/s24/tools/export_models.py
ls -la /Users/yuragms/Cursor-cash/s24/app/src/main/assets/models
```
Expected: появились `yoloe26s.onnx`, `face_det.onnx`, `face_rec.onnx`, `body_reid.onnx`, `object_encoder.onnx`. Точные имена `.pt`/выходов InsightFace проверить и поправить в скрипте при первой реальной прогонке.

- [ ] **Step 3: README инструмента**

```markdown
# tools — экспорт моделей
Запускать в venv от hikvision-test1.
1. source ../../hikvision-test1/.venv/bin/activate
2. pip install -U ultralytics
3. python export_models.py
Файлы кладутся в ../app/src/main/assets/models.
MobileCLIP image encoder экспортируется отдельно (open_clip/mobileclip репозиторий) в mobileclip_s0_image.onnx и потом подхватывается скриптом.
Имена входов/выходов ONNX зафиксировать после первого экспорта (использовать netron) и сверить с Kotlin-обёртками.
```

- [ ] **Step 4: Commit**

```bash
git add s24/tools
git commit -m "feat(s24): add model export script for ONNX assets"
```

---

## Phase 1 — Каркас Android-проекта

### Task 2: Gradle-проект и манифест

**Files:**
- Create: `s24/settings.gradle.kts`, `s24/build.gradle.kts`, `s24/gradle.properties`
- Create: `s24/app/build.gradle.kts`
- Create: `s24/app/src/main/AndroidManifest.xml`

- [ ] **Step 1: settings.gradle.kts**

```kotlin
pluginManagement { repositories { google(); mavenCentral(); gradlePluginPortal() } }
dependencyResolutionManagement { repositories { google(); mavenCentral() } }
rootProject.name = "S24Vision"
include(":app")
```

- [ ] **Step 2: app/build.gradle.kts**

```kotlin
plugins {
    id("com.android.application") version "8.5.0"
    id("org.jetbrains.kotlin.android") version "2.0.0"
}
android {
    namespace = "com.s24vision.app"
    compileSdk = 35
    defaultConfig {
        applicationId = "com.s24vision.app"
        minSdk = 28
        targetSdk = 35
        versionCode = 1
        versionName = "1.0"
    }
    buildFeatures { compose = true }
    composeOptions { kotlinCompilerExtensionVersion = "1.5.14" }
    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }
    kotlinOptions { jvmTarget = "17" }
    androidResources { noCompress += "onnx" }
}
dependencies {
    val composeBom = platform("androidx.compose:compose-bom:2024.06.00")
    implementation(composeBom)
    implementation("androidx.activity:activity-compose:1.9.0")
    implementation("androidx.compose.material3:material3")
    implementation("androidx.compose.ui:ui")
    implementation("androidx.navigation:navigation-compose:2.7.7")
    val camerax = "1.3.4"
    implementation("androidx.camera:camera-core:$camerax")
    implementation("androidx.camera:camera-camera2:$camerax")
    implementation("androidx.camera:camera-lifecycle:$camerax")
    implementation("androidx.camera:camera-view:$camerax")
    implementation("androidx.camera:camera-video:$camerax")
    implementation("com.microsoft.onnxruntime:onnxruntime-android:1.17.0")
    testImplementation("junit:junit:4.13.2")
}
```

- [ ] **Step 3: gradle.properties**

```properties
org.gradle.jvmargs=-Xmx2048m
android.useAndroidX=true
kotlin.code.style=official
```

- [ ] **Step 4: AndroidManifest.xml**

```xml
<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-feature android:name="android.hardware.camera.any" />
    <uses-permission android:name="android.permission.CAMERA" />
    <application
        android:label="S24 Vision"
        android:theme="@style/Theme.Material3.DynamicColors.DayNight"
        android:allowBackup="true">
        <activity android:name=".ui.MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
```

- [ ] **Step 5: Сборка пустого проекта**

Run: `cd /Users/yuragms/Cursor-cash/s24 && ./gradlew :app:assembleDebug`
Expected: BUILD SUCCESSFUL (после `gradle wrapper`; если wrapper отсутствует — сгенерировать `gradle wrapper --gradle-version 8.7`). Минимальный `MainActivity` добавляется в Task 12; до него допускается временная пустая Activity, чтобы собралось.

- [ ] **Step 6: Commit**

```bash
git add s24/settings.gradle.kts s24/build.gradle.kts s24/gradle.properties s24/app/build.gradle.kts s24/app/src/main/AndroidManifest.xml
git commit -m "chore(s24): scaffold android gradle project"
```

---

## Phase 2 — Ядро: математика, профили, мониторинг (TDD, JVM unit-тесты)

### Task 3: Векторная математика эмбеддингов

**Files:**
- Create: `s24/app/src/main/java/com/s24vision/app/core/math/Embeddings.kt`
- Test: `s24/app/src/test/java/com/s24vision/app/core/math/EmbeddingsTest.kt`

- [ ] **Step 1: Написать падающий тест**

```kotlin
package com.s24vision.app.core.math
import org.junit.Assert.assertEquals
import org.junit.Test

class EmbeddingsTest {
    @Test fun cosineOfIdenticalVectorsIsOne() {
        val a = floatArrayOf(1f, 2f, 2f)
        assertEquals(1.0f, Embeddings.cosine(Embeddings.l2normalize(a), Embeddings.l2normalize(a)), 1e-5f)
    }
    @Test fun cosineOfOrthogonalIsZero() {
        val a = Embeddings.l2normalize(floatArrayOf(1f, 0f))
        val b = Embeddings.l2normalize(floatArrayOf(0f, 1f))
        assertEquals(0.0f, Embeddings.cosine(a, b), 1e-5f)
    }
    @Test fun bestMatchReturnsMaxCosine() {
        val q = Embeddings.l2normalize(floatArrayOf(1f, 0f))
        val gallery = listOf(
            Embeddings.l2normalize(floatArrayOf(0f, 1f)),
            Embeddings.l2normalize(floatArrayOf(1f, 0.1f)),
        )
        assertEquals(1, Embeddings.bestMatch(q, gallery).first)
    }
}
```

- [ ] **Step 2: Запустить, убедиться что падает**

Run: `./gradlew :app:testDebugUnitTest --tests "*EmbeddingsTest*"`
Expected: FAIL (Embeddings не существует).

- [ ] **Step 3: Реализация**

```kotlin
package com.s24vision.app.core.math
import kotlin.math.sqrt

object Embeddings {
    fun l2normalize(v: FloatArray): FloatArray {
        var s = 0f; for (x in v) s += x * x
        val n = sqrt(s).coerceAtLeast(1e-12f)
        return FloatArray(v.size) { v[it] / n }
    }
    fun cosine(a: FloatArray, b: FloatArray): Float {
        var d = 0f; for (i in a.indices) d += a[i] * b[i]; return d
    }
    /** @return (индекс лучшего, косинус). Векторы предполагаются нормированными. */
    fun bestMatch(q: FloatArray, gallery: List<FloatArray>): Pair<Int, Float> {
        var bi = -1; var bs = -1f
        gallery.forEachIndexed { i, g -> val s = cosine(q, g); if (s > bs) { bs = s; bi = i } }
        return bi to bs
    }
}
```

- [ ] **Step 4: Запустить, убедиться что проходит**

Run: `./gradlew :app:testDebugUnitTest --tests "*EmbeddingsTest*"`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add s24/app/src/main/java/com/s24vision/app/core/math/Embeddings.kt s24/app/src/test/java/com/s24vision/app/core/math/EmbeddingsTest.kt
git commit -m "feat(s24): add embedding math (l2norm, cosine, bestMatch)"
```

### Task 4: Хранилище профилей (создать/дообучить + сообщения)

**Files:**
- Create: `s24/app/src/main/java/com/s24vision/app/core/profiles/Profile.kt`
- Create: `s24/app/src/main/java/com/s24vision/app/core/profiles/ProfileStore.kt`
- Test: `s24/app/src/test/java/com/s24vision/app/core/profiles/ProfileStoreTest.kt`

- [ ] **Step 1: Написать падающий тест**

```kotlin
package com.s24vision.app.core.profiles
import org.junit.Assert.*
import org.junit.Rule
import org.junit.Test
import org.junit.rules.TemporaryFolder

class ProfileStoreTest {
    @get:Rule val tmp = TemporaryFolder()

    @Test fun enrollNewCreatesProfileAndReportsCreated() {
        val store = ProfileStore(tmp.root)
        val r = store.addEmbeddings(ProfileType.OBJECT, "дрон",
            listOf(floatArrayOf(1f, 0f), floatArrayOf(0.9f, 0.1f)))
        assertTrue(r is EnrollResult.Created)
        assertEquals(2, (r as EnrollResult.Created).total)
        assertTrue(store.exists(ProfileType.OBJECT, "дрон"))
    }

    @Test fun enrollExistingMergesAndReportsImproved() {
        val store = ProfileStore(tmp.root)
        store.addEmbeddings(ProfileType.OBJECT, "дрон", listOf(floatArrayOf(1f, 0f)))
        val r = store.addEmbeddings(ProfileType.OBJECT, "дрон", listOf(floatArrayOf(0f, 1f)))
        assertTrue(r is EnrollResult.Improved)
        r as EnrollResult.Improved
        assertEquals(1, r.before); assertEquals(2, r.after)
    }

    @Test fun loadReturnsPersistedEmbeddings() {
        val store = ProfileStore(tmp.root)
        store.addEmbeddings(ProfileType.FACE, "yura", listOf(floatArrayOf(1f, 2f, 3f)))
        val store2 = ProfileStore(tmp.root)
        val p = store2.load(ProfileType.FACE, "yura")!!
        assertEquals(1, p.embeddings.size)
        assertArrayEquals(floatArrayOf(1f, 2f, 3f), p.embeddings[0], 1e-6f)
    }
}
```

- [ ] **Step 2: Запустить, убедиться что падает**

Run: `./gradlew :app:testDebugUnitTest --tests "*ProfileStoreTest*"`
Expected: FAIL (классы не существуют).

- [ ] **Step 3: Реализация Profile.kt**

```kotlin
package com.s24vision.app.core.profiles

enum class ProfileType(val dir: String) { FACE("faces"), BODY("bodies"), OBJECT("objects") }

data class Profile(
    val type: ProfileType,
    val name: String,
    val embeddings: MutableList<FloatArray>,
)

sealed interface EnrollResult {
    data class Created(val total: Int) : EnrollResult
    data class Improved(val before: Int, val after: Int) : EnrollResult
    data class Empty(val reason: String) : EnrollResult
}
```

- [ ] **Step 4: Реализация ProfileStore.kt**

```kotlin
package com.s24vision.app.core.profiles
import java.io.DataInputStream
import java.io.DataOutputStream
import java.io.File

/** Хранит профили в <root>/profiles/<type.dir>/<name>.bin (float32: [count][dim][values...]). */
class ProfileStore(root: File) {
    private val base = File(root, "profiles").apply { mkdirs() }

    private fun file(type: ProfileType, name: String) =
        File(File(base, type.dir).apply { mkdirs() }, "$name.bin")

    fun exists(type: ProfileType, name: String) = file(type, name).exists()

    fun load(type: ProfileType, name: String): Profile? {
        val f = file(type, name); if (!f.exists()) return null
        DataInputStream(f.inputStream().buffered()).use { ins ->
            val count = ins.readInt(); val dim = ins.readInt()
            val list = ArrayList<FloatArray>(count)
            repeat(count) { val a = FloatArray(dim) { ins.readFloat() }; list.add(a) }
            return Profile(type, name, list)
        }
    }

    private fun save(p: Profile) {
        val f = file(p.type, p.name)
        val dim = p.embeddings.firstOrNull()?.size ?: 0
        DataOutputStream(f.outputStream().buffered()).use { out ->
            out.writeInt(p.embeddings.size); out.writeInt(dim)
            p.embeddings.forEach { v -> v.forEach(out::writeFloat) }
        }
    }

    fun addEmbeddings(type: ProfileType, name: String, embs: List<FloatArray>): EnrollResult {
        if (embs.isEmpty()) return EnrollResult.Empty("нет эмбеддингов")
        val existing = load(type, name)
        return if (existing == null) {
            save(Profile(type, name, embs.toMutableList()))
            EnrollResult.Created(embs.size)
        } else {
            val before = existing.embeddings.size
            existing.embeddings.addAll(embs)
            save(existing)
            EnrollResult.Improved(before, existing.embeddings.size)
        }
    }

    fun list(type: ProfileType): List<String> =
        File(base, type.dir).listFiles()?.filter { it.extension == "bin" }
            ?.map { it.nameWithoutExtension } ?: emptyList()

    fun delete(type: ProfileType, name: String) = file(type, name).delete()
}
```

- [ ] **Step 5: Запустить, убедиться что проходит**

Run: `./gradlew :app:testDebugUnitTest --tests "*ProfileStoreTest*"`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add s24/app/src/main/java/com/s24vision/app/core/profiles s24/app/src/test/java/com/s24vision/app/core/profiles
git commit -m "feat(s24): add profile store with create/improve enrollment results"
```

### Task 5: Парсер мониторинга CPU

**Files:**
- Create: `s24/app/src/main/java/com/s24vision/app/core/monitor/CpuMonitor.kt`
- Test: `s24/app/src/test/java/com/s24vision/app/core/monitor/CpuMonitorParseTest.kt`

- [ ] **Step 1: Написать падающий тест (на чистой функции расчёта %)**

```kotlin
package com.s24vision.app.core.monitor
import org.junit.Assert.assertEquals
import org.junit.Test

class CpuMonitorParseTest {
    @Test fun systemPercentFromTwoSamples() {
        // total busy delta = 50, total delta = 100 -> 50%
        val prev = CpuSample(idle = 100, total = 200)
        val cur = CpuSample(idle = 150, total = 300)
        assertEquals(50f, CpuMonitor.systemPercent(prev, cur), 1e-3f)
    }
    @Test fun parseProcStatLine() {
        val line = "cpu  100 0 100 700 0 0 0 0 0 0" // user+sys=200 busy, idle=700, total=1000
        val s = CpuMonitor.parseProcStat(line)
        assertEquals(700L, s.idle)
        assertEquals(1000L, s.total)
    }
}
```

- [ ] **Step 2: Запустить, убедиться что падает**

Run: `./gradlew :app:testDebugUnitTest --tests "*CpuMonitorParseTest*"`
Expected: FAIL.

- [ ] **Step 3: Реализация**

```kotlin
package com.s24vision.app.core.monitor
import java.io.File

data class CpuSample(val idle: Long, val total: Long)

object CpuMonitor {
    fun parseProcStat(line: String): CpuSample {
        val parts = line.trim().split(Regex("\\s+")).drop(1).map { it.toLong() }
        val idle = parts.getOrElse(3) { 0 } + parts.getOrElse(4) { 0 } // idle + iowait
        val total = parts.sum()
        return CpuSample(idle, total)
    }
    fun systemPercent(prev: CpuSample, cur: CpuSample): Float {
        val dTotal = (cur.total - prev.total).toFloat()
        if (dTotal <= 0f) return 0f
        val dIdle = (cur.idle - prev.idle).toFloat()
        return ((dTotal - dIdle) / dTotal * 100f).coerceIn(0f, 100f)
    }
    fun readSystem(): CpuSample =
        parseProcStat(File("/proc/stat").bufferedReader().use { it.readLine() })
}
```

- [ ] **Step 4: Запустить, убедиться что проходит**

Run: `./gradlew :app:testDebugUnitTest --tests "*CpuMonitorParseTest*"`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add s24/app/src/main/java/com/s24vision/app/core/monitor/CpuMonitor.kt s24/app/src/test/java/com/s24vision/app/core/monitor/CpuMonitorParseTest.kt
git commit -m "feat(s24): add CPU usage monitor with proc/stat parsing"
```

### Task 6: GPU монитор (best-effort, без unit-теста)

**Files:**
- Create: `s24/app/src/main/java/com/s24vision/app/core/monitor/GpuMonitor.kt`

- [ ] **Step 1: Реализация**

```kotlin
package com.s24vision.app.core.monitor
import java.io.File

/** Adreno: /sys/class/kgsl/kgsl-3d0/gpu_busy_percentage (если доступно). Иначе null -> "n/a". */
object GpuMonitor {
    private val candidates = listOf(
        "/sys/class/kgsl/kgsl-3d0/gpu_busy_percentage",
        "/sys/class/kgsl/kgsl-3d0/gpubusy",
    )
    fun percent(): Float? {
        for (p in candidates) {
            val f = File(p); if (!f.canRead()) continue
            val raw = runCatching { f.readText().trim() }.getOrNull() ?: continue
            // gpu_busy_percentage -> "37 %"; gpubusy -> "busy total" два числа
            val nums = Regex("\\d+").findAll(raw).map { it.value.toLong() }.toList()
            if (raw.contains("%") && nums.isNotEmpty()) return nums[0].toFloat().coerceIn(0f, 100f)
            if (nums.size >= 2 && nums[1] > 0) return (nums[0].toFloat() / nums[1] * 100f).coerceIn(0f, 100f)
        }
        return null
    }
    fun label(): String = percent()?.let { "GPU: ${"%.0f".format(it)}%" } ?: "GPU: n/a"
}
```

- [ ] **Step 2: Commit**

```bash
git add s24/app/src/main/java/com/s24vision/app/core/monitor/GpuMonitor.kt
git commit -m "feat(s24): add best-effort Adreno GPU monitor"
```

### Task 7: Формула суммарной точности (порт из hikvision)

**Files:**
- Create: `s24/app/src/main/java/com/s24vision/app/vision/IdentityFusion.kt`
- Test: `s24/app/src/test/java/com/s24vision/app/vision/IdentityFusionTest.kt`

- [ ] **Step 1: Падающий тест (значения из README hikvision)**

```kotlin
package com.s24vision.app.vision
import org.junit.Assert.assertEquals
import org.junit.Test

class IdentityFusionTest {
    private val f = IdentityFusion(faceTh = 0.45f, bodyTh = 0.65f)
    @Test fun bothChannelsAgree() {
        // 0.6*0.5 + 0.4*0.7 + 0.2*min = 0.3+0.28+0.2*0.5=0.68
        assertEquals(0.68f, f.total(face = 0.5f, body = 0.7f), 1e-3f)
    }
    @Test fun weakFaceStrongBody() {
        // один канал: max + 0.25*min = 0.72 + 0.25*0.31 = 0.7975
        assertEquals(0.7975f, f.total(face = 0.31f, body = 0.72f), 1e-3f)
    }
}
```

- [ ] **Step 2: Запустить, убедиться что падает**

Run: `./gradlew :app:testDebugUnitTest --tests "*IdentityFusionTest*"`
Expected: FAIL.

- [ ] **Step 3: Реализация**

```kotlin
package com.s24vision.app.vision
import kotlin.math.max
import kotlin.math.min

class IdentityFusion(
    private val faceTh: Float, private val bodyTh: Float,
    private val faceW: Float = 0.6f, private val bodyW: Float = 0.4f,
    private val agreeBonus: Float = 0.2f, private val weakBlend: Float = 0.25f,
) {
    fun total(face: Float, body: Float): Float {
        val bothOk = face >= faceTh && body >= bodyTh
        return if (bothOk)
            min(1f, faceW * face + bodyW * body + agreeBonus * min(face, body))
        else
            min(1f, max(face, body) + weakBlend * min(face, body))
    }
}
```

- [ ] **Step 4: Запустить, убедиться что проходит**

Run: `./gradlew :app:testDebugUnitTest --tests "*IdentityFusionTest*"`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add s24/app/src/main/java/com/s24vision/app/vision/IdentityFusion.kt s24/app/src/test/java/com/s24vision/app/vision/IdentityFusionTest.kt
git commit -m "feat(s24): port identity fusion total formula from hikvision"
```

---

## Phase 3 — ONNX-обёртка и vision-модули (проверка на устройстве)

> Эти модули зависят от реальных моделей и API ONNX Runtime; unit-тесты на JVM без нативной библиотеки невозможны. Каждая задача завершается сборкой + проверкой на устройстве через лог.

### Task 8: Обёртка OnnxModel

**Files:**
- Create: `s24/app/src/main/java/com/s24vision/app/core/onnx/OnnxModel.kt`

- [ ] **Step 1: Реализация**

```kotlin
package com.s24vision.app.core.onnx
import ai.onnxruntime.OnnxTensor
import ai.onnxruntime.OrtEnvironment
import ai.onnxruntime.OrtSession
import ai.onnxruntime.providers.NNAPIFlags
import android.content.Context
import java.nio.FloatBuffer
import java.util.EnumSet

class OnnxModel(context: Context, assetName: String, useNnapi: Boolean = true) {
    private val env: OrtEnvironment = OrtEnvironment.getEnvironment()
    private val session: OrtSession

    init {
        val bytes = context.assets.open("models/$assetName").readBytes()
        val opts = OrtSession.SessionOptions().apply {
            if (useNnapi) runCatching { addNnapi(EnumSet.of(NNAPIFlags.USE_FP16)) }
        }
        session = env.createSession(bytes, opts)
    }

    val inputName: String get() = session.inputNames.iterator().next()

    /** Прогон одного входа NCHW. Возвращает первый выход как FloatArray + его форму. */
    fun run(input: FloatArray, shape: LongArray): Pair<FloatArray, LongArray> {
        OnnxTensor.createTensor(env, FloatBuffer.wrap(input), shape).use { t ->
            session.run(mapOf(inputName to t)).use { res ->
                val out = res[0] as OnnxTensor
                val info = out.info.shape
                val buf = out.floatBuffer
                val arr = FloatArray(buf.remaining()); buf.get(arr)
                return arr to info
            }
        }
    }
    fun close() { session.close() }
}
```

- [ ] **Step 2: Сборка**

Run: `cd /Users/yuragms/Cursor-cash/s24 && ./gradlew :app:assembleDebug`
Expected: BUILD SUCCESSFUL.

- [ ] **Step 3: Commit**

```bash
git add s24/app/src/main/java/com/s24vision/app/core/onnx/OnnxModel.kt
git commit -m "feat(s24): add ONNX Runtime model wrapper with NNAPI"
```

### Task 9: YOLOE детектор

**Files:**
- Create: `s24/app/src/main/java/com/s24vision/app/vision/Detection.kt`
- Create: `s24/app/src/main/java/com/s24vision/app/vision/YoloeDetector.kt`

- [ ] **Step 1: Detection.kt**

```kotlin
package com.s24vision.app.vision
import android.graphics.RectF

data class Detection(
    val rect: RectF,      // в координатах исходного кадра
    val label: String,    // тип от модели или "person"
    val score: Float,
    val isPerson: Boolean,
)
```

- [ ] **Step 2: YoloeDetector.kt — препроцесс/инференс/постобработка**

```kotlin
package com.s24vision.app.vision
import android.content.Context
import android.graphics.Bitmap
import android.graphics.RectF
import com.s24vision.app.core.onnx.OnnxModel

/** prompt-free YOLOE-26. Постобработка под NMS-free end2end выход.
 *  ВНИМАНИЕ: точная форма выхода/раскладка классов фиксируется по netron после
 *  экспорта (Task 1) и при необходимости правится здесь. */
class YoloeDetector(context: Context, private val inputSize: Int = 640) {
    private val model = OnnxModel(context, "yoloe26s.onnx")

    fun detect(bitmap: Bitmap, confTh: Float): List<Detection> {
        val scaled = Bitmap.createScaledBitmap(bitmap, inputSize, inputSize, true)
        val input = bitmapToNchw(scaled)
        val (out, shape) = model.run(input, longArrayOf(1, 3, inputSize.toLong(), inputSize.toLong()))
        return decode(out, shape, bitmap.width, bitmap.height, confTh)
    }

    private fun bitmapToNchw(bmp: Bitmap): FloatArray {
        val w = bmp.width; val h = bmp.height
        val px = IntArray(w * h); bmp.getPixels(px, 0, w, 0, 0, w, h)
        val out = FloatArray(3 * w * h)
        val plane = w * h
        for (i in 0 until plane) {
            val p = px[i]
            out[i] = ((p shr 16 and 0xFF) / 255f)
            out[plane + i] = ((p shr 8 and 0xFF) / 255f)
            out[2 * plane + i] = ((p and 0xFF) / 255f)
        }
        return out
    }

    /** Декод заполняется по реальной форме выхода (после netron). Боксы масштабируются
     *  обратно к исходному кадру. Здесь — каркас под формат [n,6]=(x1,y1,x2,y2,score,cls). */
    private fun decode(out: FloatArray, shape: LongArray, ow: Int, oh: Int, confTh: Float): List<Detection> {
        val res = ArrayList<Detection>()
        val stride = 6
        val n = out.size / stride
        val sx = ow.toFloat() / inputSize; val sy = oh.toFloat() / inputSize
        for (i in 0 until n) {
            val o = i * stride
            val score = out[o + 4]; if (score < confTh) continue
            val cls = out[o + 5].toInt()
            val label = CocoLabels.nameOf(cls)
            res.add(Detection(
                RectF(out[o] * sx, out[o + 1] * sy, out[o + 2] * sx, out[o + 3] * sy),
                label, score, isPerson = label == "person",
            ))
        }
        return res
    }
    fun close() = model.close()
}
```

- [ ] **Step 3: CocoLabels — отображение id→имя (минимально person)**

```kotlin
// в Detection.kt или отдельном файле vision/CocoLabels.kt
package com.s24vision.app.vision
object CocoLabels {
    // Полный словарь YOLOE prompt-free фиксируется после экспорта; здесь ключевое.
    private val map = mapOf(0 to "person")
    fun nameOf(id: Int): String = map[id] ?: "object"
}
```

- [ ] **Step 4: Сборка + устройство**

Run: `./gradlew :app:assembleDebug`
Expected: BUILD SUCCESSFUL. Реальная проверка боксов — на устройстве (Task 12) с `adb logcat` (логировать число детекций).

- [ ] **Step 5: Commit**

```bash
git add s24/app/src/main/java/com/s24vision/app/vision/Detection.kt s24/app/src/main/java/com/s24vision/app/vision/YoloeDetector.kt s24/app/src/main/java/com/s24vision/app/vision/CocoLabels.kt
git commit -m "feat(s24): add YOLOE detector (preprocess/infer/decode skeleton)"
```

### Task 10: Распознаватели лица, тела и объекта

**Files:**
- Create: `s24/app/src/main/java/com/s24vision/app/vision/FaceRecognizer.kt`
- Create: `s24/app/src/main/java/com/s24vision/app/vision/BodyRecognizer.kt`
- Create: `s24/app/src/main/java/com/s24vision/app/vision/ObjectRecognizer.kt`

- [ ] **Step 1: BodyRecognizer.kt (OSNet, 256x128)**

```kotlin
package com.s24vision.app.vision
import android.content.Context
import android.graphics.Bitmap
import com.s24vision.app.core.math.Embeddings
import com.s24vision.app.core.onnx.OnnxModel

class BodyRecognizer(context: Context) {
    private val model = OnnxModel(context, "body_reid.onnx")
    private val W = 128; private val H = 256
    private val mean = floatArrayOf(0.485f, 0.456f, 0.406f)
    private val std = floatArrayOf(0.229f, 0.224f, 0.225f)

    fun embed(crop: Bitmap): FloatArray {
        val b = Bitmap.createScaledBitmap(crop, W, H, true)
        val px = IntArray(W * H); b.getPixels(px, 0, W, 0, 0, W, H)
        val input = FloatArray(3 * W * H); val plane = W * H
        for (i in 0 until plane) {
            val p = px[i]
            input[i] = (((p shr 16 and 0xFF) / 255f) - mean[0]) / std[0]
            input[plane + i] = (((p shr 8 and 0xFF) / 255f) - mean[1]) / std[1]
            input[2 * plane + i] = (((p and 0xFF) / 255f) - mean[2]) / std[2]
        }
        val (out, _) = model.run(input, longArrayOf(1, 3, H.toLong(), W.toLong()))
        return Embeddings.l2normalize(out)
    }
    fun close() = model.close()
}
```

- [ ] **Step 2: ObjectRecognizer.kt (MobileCLIP) + матч с профилями**

```kotlin
package com.s24vision.app.vision
import android.content.Context
import android.graphics.Bitmap
import com.s24vision.app.core.math.Embeddings
import com.s24vision.app.core.onnx.OnnxModel
import com.s24vision.app.core.profiles.Profile

class ObjectRecognizer(context: Context, private val size: Int = 256) {
    private val model = OnnxModel(context, "object_encoder.onnx")

    fun embed(crop: Bitmap): FloatArray {
        val b = Bitmap.createScaledBitmap(crop, size, size, true)
        val px = IntArray(size * size); b.getPixels(px, 0, size, 0, 0, size, size)
        val input = FloatArray(3 * size * size); val plane = size * size
        for (i in 0 until plane) {
            val p = px[i]
            input[i] = (p shr 16 and 0xFF) / 255f
            input[plane + i] = (p shr 8 and 0xFF) / 255f
            input[2 * plane + i] = (p and 0xFF) / 255f
        }
        val (out, _) = model.run(input, longArrayOf(1, 3, size.toLong(), size.toLong()))
        return Embeddings.l2normalize(out)
    }

    /** @return имя профиля или null, если ниже порога. */
    fun match(crop: Bitmap, profiles: List<Profile>, th: Float): Pair<String, Float>? {
        if (profiles.isEmpty()) return null
        val q = embed(crop)
        var best: Pair<String, Float>? = null
        for (p in profiles) {
            val (_, s) = Embeddings.bestMatch(q, p.embeddings)
            if (s >= th && (best == null || s > best!!.second)) best = p.name to s
        }
        return best
    }
    fun close() = model.close()
}
```

- [ ] **Step 3: FaceRecognizer.kt (SCRFD детект + 5 точек + ArcFace)**

```kotlin
package com.s24vision.app.vision
import android.content.Context
import android.graphics.Bitmap
import com.s24vision.app.core.math.Embeddings
import com.s24vision.app.core.onnx.OnnxModel

/** Детекция лица (SCRFD) внутри кропа человека, выравнивание по 5 точкам,
 *  эмбеддинг ArcFace 112x112. Постобработка SCRFD и матрица выравнивания
 *  фиксируются по форме выхода face_det.onnx (netron) на этапе устройства. */
class FaceRecognizer(context: Context) {
    private val det = OnnxModel(context, "face_det.onnx")
    private val rec = OnnxModel(context, "face_rec.onnx")
    private val faceSize = 112

    /** Возвращает эмбеддинг лица или null, если лицо не найдено. */
    fun embed(personCrop: Bitmap): FloatArray? {
        val aligned = detectAndAlign(personCrop) ?: return null
        val px = IntArray(faceSize * faceSize)
        aligned.getPixels(px, 0, faceSize, 0, 0, faceSize, faceSize)
        val input = FloatArray(3 * faceSize * faceSize); val plane = faceSize * faceSize
        for (i in 0 until plane) {
            val p = px[i]
            input[i] = (((p shr 16 and 0xFF) / 255f) - 0.5f) / 0.5f
            input[plane + i] = (((p shr 8 and 0xFF) / 255f) - 0.5f) / 0.5f
            input[2 * plane + i] = (((p and 0xFF) / 255f) - 0.5f) / 0.5f
        }
        val (out, _) = rec.run(input, longArrayOf(1, 3, faceSize.toLong(), faceSize.toLong()))
        return Embeddings.l2normalize(out)
    }

    /** Каркас: запуск SCRFD, выбор крупнейшего лица, аффинное выравнивание по 5 точкам.
     *  Точная декодировка боксов/ландмарков заполняется по выходу face_det.onnx. */
    private fun detectAndAlign(crop: Bitmap): Bitmap? {
        // TODO-устройство: декодировать SCRFD, при отсутствии лица вернуть null,
        // иначе выровнять и вернуть Bitmap 112x112. До отладки на устройстве —
        // временно центр-кроп ресайз 112 (даёт грубый эмбеддинг).
        return Bitmap.createScaledBitmap(crop, faceSize, faceSize, true)
    }
    fun close() { det.close(); rec.close() }
}
```

> Примечание плана: метод `detectAndAlign` помечен как требующий доводки по реальному выходу `face_det.onnx`; временная реализация позволяет собрать и протестировать остальной пайплайн. Это единственный осознанный TODO, привязанный к артефакту, который доступен только после экспорта моделей.

- [ ] **Step 4: Сборка**

Run: `./gradlew :app:assembleDebug`
Expected: BUILD SUCCESSFUL.

- [ ] **Step 5: Commit**

```bash
git add s24/app/src/main/java/com/s24vision/app/vision/FaceRecognizer.kt s24/app/src/main/java/com/s24vision/app/vision/BodyRecognizer.kt s24/app/src/main/java/com/s24vision/app/vision/ObjectRecognizer.kt
git commit -m "feat(s24): add face/body/object recognizers"
```

### Task 11: FramePipeline — связывание детекции и распознавания

**Files:**
- Create: `s24/app/src/main/java/com/s24vision/app/vision/FramePipeline.kt`

- [ ] **Step 1: Реализация**

```kotlin
package com.s24vision.app.vision
import android.graphics.Bitmap
import com.s24vision.app.core.profiles.Profile
import com.s24vision.app.core.profiles.ProfileStore
import com.s24vision.app.core.profiles.ProfileType
import com.s24vision.app.core.math.Embeddings

data class Annotation(val det: Detection, val displayLabel: String)

class FramePipeline(
    private val detector: YoloeDetector,
    private val face: FaceRecognizer,
    private val body: BodyRecognizer,
    private val obj: ObjectRecognizer,
    private val store: ProfileStore,
    private val fusion: IdentityFusion,
    private val confTh: Float = 0.4f,
    private val faceTh: Float = 0.45f,
    private val bodyTh: Float = 0.65f,
    private val objTh: Float = 0.5f,
) {
    private fun crop(bmp: Bitmap, d: Detection): Bitmap {
        val l = d.rect.left.toInt().coerceIn(0, bmp.width - 1)
        val t = d.rect.top.toInt().coerceIn(0, bmp.height - 1)
        val r = d.rect.right.toInt().coerceIn(l + 1, bmp.width)
        val b = d.rect.bottom.toInt().coerceIn(t + 1, bmp.height)
        return Bitmap.createBitmap(bmp, l, t, r - l, b - t)
    }

    fun process(bmp: Bitmap): List<Annotation> {
        val faces = store.list(ProfileType.FACE).mapNotNull { store.load(ProfileType.FACE, it) }
        val bodies = store.list(ProfileType.BODY).mapNotNull { store.load(ProfileType.BODY, it) }
        val objs = store.list(ProfileType.OBJECT).mapNotNull { store.load(ProfileType.OBJECT, it) }
        return detector.detect(bmp, confTh).map { d ->
            val label = if (d.isPerson) personLabel(crop(bmp, d), faces, bodies)
                        else objectLabel(crop(bmp, d), d, objs)
            Annotation(d, label)
        }
    }

    private fun bestPerName(q: FloatArray?, profiles: List<Profile>): Pair<String, Float>? {
        if (q == null) return null
        var best: Pair<String, Float>? = null
        for (p in profiles) {
            val (_, s) = Embeddings.bestMatch(q, p.embeddings)
            if (best == null || s > best!!.second) best = p.name to s
        }
        return best
    }

    private fun personLabel(c: Bitmap, faces: List<Profile>, bodies: List<Profile>): String {
        val fe = face.embed(c)
        val be = body.embed(c)
        val fm = bestPerName(fe, faces)
        val bm = bestPerName(be, bodies)
        val name = fm?.takeIf { it.second >= faceTh }?.first
            ?: bm?.takeIf { it.second >= bodyTh }?.first ?: return "person"
        val faceS = if (fm?.first == name) fm.second else 0f
        val bodyS = if (bm?.first == name) bm.second else 0f
        val total = fusion.total(faceS, bodyS)
        return "%s face:%.2f body:%.2f total:%.2f".format(name, faceS, bodyS, total)
    }

    private fun objectLabel(c: Bitmap, d: Detection, objs: List<Profile>): String {
        val m = obj.match(c, objs, objTh)
        if (m != null) return m.first
        return if (d.label == "object") "неопознанный объект" else d.label
    }
}
```

- [ ] **Step 2: Сборка**

Run: `./gradlew :app:assembleDebug`
Expected: BUILD SUCCESSFUL.

- [ ] **Step 3: Commit**

```bash
git add s24/app/src/main/java/com/s24vision/app/vision/FramePipeline.kt
git commit -m "feat(s24): add frame pipeline tying detection and recognition"
```

---

## Phase 4 — Камера, запись, обучение, UI

### Task 12: CameraX + базовый экран камеры + разрешения

**Files:**
- Create: `s24/app/src/main/java/com/s24vision/app/camera/CameraController.kt`
- Create: `s24/app/src/main/java/com/s24vision/app/ui/MainActivity.kt`
- Create: `s24/app/src/main/java/com/s24vision/app/ui/CameraScreen.kt`
- Create: `s24/app/src/main/java/com/s24vision/app/ui/OverlayView.kt`

- [ ] **Step 1: CameraController.kt — превью + ImageAnalysis + переключение камеры**

```kotlin
package com.s24vision.app.camera
import android.content.Context
import androidx.camera.core.CameraSelector
import androidx.camera.core.ImageAnalysis
import androidx.camera.core.ImageProxy
import androidx.camera.core.Preview
import androidx.camera.lifecycle.ProcessCameraProvider
import androidx.camera.view.PreviewView
import androidx.core.content.ContextCompat
import androidx.lifecycle.LifecycleOwner
import java.util.concurrent.Executors

class CameraController(private val context: Context) {
    private var useFront = false
    private val analysisExecutor = Executors.newSingleThreadExecutor()
    private var provider: ProcessCameraProvider? = null

    fun start(owner: LifecycleOwner, previewView: PreviewView, onFrame: (ImageProxy) -> Unit) {
        val future = ProcessCameraProvider.getInstance(context)
        future.addListener({
            provider = future.get()
            bind(owner, previewView, onFrame)
        }, ContextCompat.getMainExecutor(context))
    }

    fun toggleLens(owner: LifecycleOwner, previewView: PreviewView, onFrame: (ImageProxy) -> Unit) {
        useFront = !useFront; bind(owner, previewView, onFrame)
    }

    private fun bind(owner: LifecycleOwner, previewView: PreviewView, onFrame: (ImageProxy) -> Unit) {
        val p = provider ?: return
        p.unbindAll()
        val selector = if (useFront) CameraSelector.DEFAULT_FRONT_CAMERA else CameraSelector.DEFAULT_BACK_CAMERA
        val preview = Preview.Builder().build().also { it.setSurfaceProvider(previewView.surfaceProvider) }
        val analysis = ImageAnalysis.Builder()
            .setBackpressureStrategy(ImageAnalysis.STRATEGY_KEEP_ONLY_LATEST).build()
            .also { it.setAnalyzer(analysisExecutor) { proxy -> onFrame(proxy) } }
        p.bindToLifecycle(owner, selector, preview, analysis)
    }
}
```

- [ ] **Step 2: MainActivity.kt — запрос CAMERA + навигация**

```kotlin
package com.s24vision.app.ui
import android.os.Bundle
import androidx.activity.ComponentActivity
import androidx.activity.compose.setContent
import androidx.activity.result.contract.ActivityResultContracts
import androidx.compose.runtime.*

class MainActivity : ComponentActivity() {
    private val granted = mutableStateOf(false)
    private val req = registerForActivityResult(ActivityResultContracts.RequestPermission()) {
        granted.value = it
    }
    override fun onCreate(s: Bundle?) {
        super.onCreate(s)
        req.launch(android.Manifest.permission.CAMERA)
        setContent { if (granted.value) AppNav() else PermissionPrompt() }
    }
}
```

- [ ] **Step 3: OverlayView.kt — рисование боксов**

```kotlin
package com.s24vision.app.ui
import androidx.compose.foundation.Canvas
import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier
import androidx.compose.ui.geometry.Offset
import androidx.compose.ui.geometry.Size
import androidx.compose.ui.graphics.Color
import com.s24vision.app.vision.Annotation

@Composable
fun OverlayView(annotations: List<Annotation>, modifier: Modifier = Modifier) {
    Canvas(modifier) {
        annotations.forEach { a ->
            val r = a.det.rect
            val color = if (a.det.isPerson) Color.Cyan else Color.Green
            drawRect(color = color, topLeft = Offset(r.left, r.top),
                size = Size(r.width(), r.height()), alpha = 1f, style = androidx.compose.ui.graphics.drawscope.Stroke(3f))
        }
    }
}
```

- [ ] **Step 4: CameraScreen.kt — превью + overlay + оверлей нагрузки + REC**

```kotlin
package com.s24vision.app.ui
import androidx.camera.view.PreviewView
import androidx.compose.foundation.layout.*
import androidx.compose.material3.*
import androidx.compose.runtime.*
import androidx.compose.ui.Modifier
import androidx.compose.ui.viewinterop.AndroidView
import com.s24vision.app.vision.Annotation

@Composable
fun CameraScreen(state: CameraScreenState) {
    Box(Modifier.fillMaxSize()) {
        AndroidView(factory = { ctx -> PreviewView(ctx).also(state::attachPreview) }, modifier = Modifier.fillMaxSize())
        OverlayView(state.annotations.value, Modifier.fillMaxSize())
        Text(state.statsLabel.value, modifier = Modifier.padding(8.dp))
        Row(Modifier.align(androidx.compose.ui.Alignment.BottomCenter).padding(16.dp)) {
            Button(onClick = state::toggleLens) { Text("Камера") }
            Spacer(Modifier.width(12.dp))
            Button(onClick = state::toggleRecord) { Text(if (state.recording.value) "Стоп" else "REC") }
        }
    }
}
```

> `CameraScreenState` (holder с CameraController, FramePipeline, мониторами, FrameRecorder, состояниями `annotations/statsLabel/recording`) создаётся в `AppNav` (Task 15). Поля: `attachPreview(view)`, `toggleLens()`, `toggleRecord()`, `annotations: State<List<Annotation>>`, `statsLabel: State<String>`, `recording: State<Boolean>`.

- [ ] **Step 5: Сборка + установка на устройство**

Run:
```bash
cd /Users/yuragms/Cursor-cash/s24
./gradlew :app:installDebug
adb shell am start -n com.s24vision.app/.ui.MainActivity
adb logcat -s S24Vision:* *:E
```
Expected: приложение открывается, запрашивает камеру, показывает превью. (Логику кадров подключаем в Task 15.)

- [ ] **Step 6: Commit**

```bash
git add s24/app/src/main/java/com/s24vision/app/camera s24/app/src/main/java/com/s24vision/app/ui/MainActivity.kt s24/app/src/main/java/com/s24vision/app/ui/CameraScreen.kt s24/app/src/main/java/com/s24vision/app/ui/OverlayView.kt
git commit -m "feat(s24): add CameraX controller and camera screen scaffold"
```

### Task 13: Запись аннотированных кадров

**Files:**
- Create: `s24/app/src/main/java/com/s24vision/app/record/FrameRecorder.kt`

- [ ] **Step 1: Реализация (MediaCodec + MediaMuxer, кадры с нарисованными боксами)**

```kotlin
package com.s24vision.app.record
import android.graphics.Bitmap
import android.media.MediaCodec
import android.media.MediaCodecInfo
import android.media.MediaFormat
import android.media.MediaMuxer
import java.io.File

/** Кодирует последовательность Bitmap (уже с нарисованными боксами) в H.264 mp4. */
class FrameRecorder(private val width: Int, private val height: Int, private val fps: Int = 15) {
    private lateinit var codec: MediaCodec
    private lateinit var muxer: MediaMuxer
    private var trackIndex = -1
    private var started = false
    private var ptsUs = 0L
    lateinit var outputFile: File; private set

    fun start(dir: File) {
        dir.mkdirs()
        val ts = java.text.SimpleDateFormat("yyyy-MM-dd_HH-mm-ss", java.util.Locale.US).format(java.util.Date())
        outputFile = File(dir, "$ts.mp4")
        val fmt = MediaFormat.createVideoFormat(MediaFormat.MIMETYPE_VIDEO_AVC, width, height).apply {
            setInteger(MediaFormat.KEY_COLOR_FORMAT, MediaCodecInfo.CodecCapabilities.COLOR_FormatSurface)
            setInteger(MediaFormat.KEY_BIT_RATE, 6_000_000)
            setInteger(MediaFormat.KEY_FRAME_RATE, fps)
            setInteger(MediaFormat.KEY_I_FRAME_INTERVAL, 1)
        }
        codec = MediaCodec.createEncoderByType(MediaFormat.MIMETYPE_VIDEO_AVC)
        codec.configure(fmt, null, null, MediaCodec.CONFIGURE_FLAG_ENCODE)
        // Для простоты используем input Surface + Canvas; подробности рендеринга кадра
        // на Surface реализуются здесь.
        muxer = MediaMuxer(outputFile.path, MediaMuxer.OutputFormat.MUXER_OUTPUT_MPEG_4)
        codec.start()
    }

    fun encode(frame: Bitmap) {
        // Рисуем frame на input surface кодека (lockCanvas/draw/unlock) и дренируем выход в muxer.
        // Реализация дренажа стандартная для MediaCodec encoder.
        ptsUs += 1_000_000L / fps
    }

    fun stop() {
        if (started) { codec.stop(); muxer.stop() }
        codec.release(); muxer.release()
    }
}
```

> Примечание: рендеринг Bitmap на input-Surface и дренаж буферов — стандартная процедура MediaCodec; детально дорабатывается и проверяется на устройстве (запись короткого ролика и открытие файла). На этом шаге фиксируется интерфейс `start/encode/stop` и путь файла.

- [ ] **Step 2: Сборка**

Run: `./gradlew :app:assembleDebug`
Expected: BUILD SUCCESSFUL.

- [ ] **Step 3: Commit**

```bash
git add s24/app/src/main/java/com/s24vision/app/record/FrameRecorder.kt
git commit -m "feat(s24): add frame recorder (MediaCodec mp4)"
```

### Task 14: Enroller — обучение/дообучение по видео

**Files:**
- Create: `s24/app/src/main/java/com/s24vision/app/enroll/Enroller.kt`

- [ ] **Step 1: Реализация (сэмплинг кадров MediaMetadataRetriever + сбор эмбеддингов)**

```kotlin
package com.s24vision.app.enroll
import android.graphics.Bitmap
import android.media.MediaMetadataRetriever
import com.s24vision.app.core.profiles.EnrollResult
import com.s24vision.app.core.profiles.ProfileStore
import com.s24vision.app.core.profiles.ProfileType
import com.s24vision.app.vision.*

class Enroller(
    private val detector: YoloeDetector,
    private val face: FaceRecognizer,
    private val body: BodyRecognizer,
    private val obj: ObjectRecognizer,
    private val store: ProfileStore,
    private val confTh: Float = 0.4f,
    private val sampleEveryMs: Long = 300,
    private val maxSamples: Int = 50,
) {
    data class Outcome(val face: EnrollResult?, val body: EnrollResult?, val obj: EnrollResult?)

    /** isPerson=true -> лицо+тело; false -> объект (берётся крупнейший бокс). */
    fun enroll(videoPath: String, name: String, isPerson: Boolean): Outcome {
        val frames = sampleFrames(videoPath)
        val faceEmbs = ArrayList<FloatArray>(); val bodyEmbs = ArrayList<FloatArray>()
        val objEmbs = ArrayList<FloatArray>()
        for (f in frames) {
            val dets = detector.detect(f, confTh)
            if (isPerson) {
                val person = dets.filter { it.isPerson }.maxByOrNull { it.rect.width() * it.rect.height() } ?: continue
                val crop = cropOf(f, person)
                face.embed(crop)?.let { faceEmbs.add(it) }
                bodyEmbs.add(body.embed(crop))
            } else {
                val o = dets.filter { !it.isPerson }.maxByOrNull { it.rect.width() * it.rect.height() } ?: continue
                objEmbs.add(obj.embed(cropOf(f, o)))
            }
        }
        return if (isPerson) Outcome(
            store.addEmbeddings(ProfileType.FACE, name, faceEmbs),
            store.addEmbeddings(ProfileType.BODY, name, bodyEmbs), null)
        else Outcome(null, null, store.addEmbeddings(ProfileType.OBJECT, name, objEmbs))
    }

    private fun cropOf(bmp: Bitmap, d: Detection): Bitmap {
        val l = d.rect.left.toInt().coerceIn(0, bmp.width - 1)
        val t = d.rect.top.toInt().coerceIn(0, bmp.height - 1)
        val r = d.rect.right.toInt().coerceIn(l + 1, bmp.width)
        val b = d.rect.bottom.toInt().coerceIn(t + 1, bmp.height)
        return Bitmap.createBitmap(bmp, l, t, r - l, b - t)
    }

    private fun sampleFrames(path: String): List<Bitmap> {
        val r = MediaMetadataRetriever(); r.setDataSource(path)
        val durMs = r.extractMetadata(MediaMetadataRetriever.METADATA_KEY_DURATION)?.toLong() ?: 0
        val out = ArrayList<Bitmap>()
        var t = 0L
        while (t < durMs && out.size < maxSamples) {
            r.getFrameAtTime(t * 1000, MediaMetadataRetriever.OPTION_CLOSEST)?.let { out.add(it) }
            t += sampleEveryMs
        }
        r.release(); return out
    }
}
```

- [ ] **Step 2: Сборка**

Run: `./gradlew :app:assembleDebug`
Expected: BUILD SUCCESSFUL.

- [ ] **Step 3: Commit**

```bash
git add s24/app/src/main/java/com/s24vision/app/enroll/Enroller.kt
git commit -m "feat(s24): add enroller for person/object profiles from video"
```

### Task 15: Навигация + связывание состояния (живой пайплайн, мониторинг, запись)

**Files:**
- Create: `s24/app/src/main/java/com/s24vision/app/ui/Nav.kt`
- Create: `s24/app/src/main/java/com/s24vision/app/ui/CameraScreenState.kt`

- [ ] **Step 1: CameraScreenState — обработка кадра, оверлей нагрузки**

```kotlin
package com.s24vision.app.ui
import androidx.camera.core.ImageProxy
import androidx.camera.view.PreviewView
import androidx.compose.runtime.mutableStateOf
import androidx.lifecycle.LifecycleOwner
import com.s24vision.app.camera.CameraController
import com.s24vision.app.core.monitor.CpuMonitor
import com.s24vision.app.core.monitor.GpuMonitor
import com.s24vision.app.vision.Annotation
import com.s24vision.app.vision.FramePipeline

class CameraScreenState(
    private val owner: LifecycleOwner,
    private val controller: CameraController,
    private val pipeline: FramePipeline,
) {
    val annotations = mutableStateOf<List<Annotation>>(emptyList())
    val statsLabel = mutableStateOf("")
    val recording = mutableStateOf(false)
    private var preview: PreviewView? = null
    private var prevCpu = CpuMonitor.readSystem()
    private var frameCount = 0

    fun attachPreview(view: PreviewView) { preview = view; controller.start(owner, view, ::onFrame) }
    fun toggleLens() { preview?.let { controller.toggleLens(owner, it, ::onFrame) } }
    fun toggleRecord() { recording.value = !recording.value }

    private fun onFrame(proxy: ImageProxy) {
        try {
            frameCount++
            if (frameCount % 3 == 0) { // каждый 3-й кадр — YOLOE
                val bmp = proxy.toBitmapCompat()
                annotations.value = pipeline.process(bmp)
                val cur = CpuMonitor.readSystem()
                val cpu = CpuMonitor.systemPercent(prevCpu, cur); prevCpu = cur
                statsLabel.value = "CPU: %.0f%%  %s".format(cpu, GpuMonitor.label())
            }
        } finally { proxy.close() }
    }
}
```

> `ImageProxy.toBitmapCompat()` — extension, конвертирующая YUV→Bitmap (стандартная реализация через `YuvImage`/`RenderScript`-замену; добавить в `camera/ImageProxyExt.kt`). Если запись активна — кадр с боксами также отдаётся `FrameRecorder` (подключается здесь же).

- [ ] **Step 2: Nav.kt — четыре экрана**

```kotlin
package com.s24vision.app.ui
import androidx.compose.runtime.Composable
import androidx.navigation.compose.NavHost
import androidx.navigation.compose.composable
import androidx.navigation.compose.rememberNavController

@Composable
fun AppNav() {
    val nav = rememberNavController()
    NavHost(nav, startDestination = "camera") {
        composable("camera") { /* CameraScreen(state) — state создаётся через remember с DI */ }
        composable("recordings") { RecordingsScreen(nav) }
        composable("train") { TrainScreen() }
        composable("profiles") { ProfilesScreen() }
    }
}
```

- [ ] **Step 3: Сборка + устройство (живая детекция и оверлей нагрузки)**

Run: `./gradlew :app:installDebug && adb logcat -s S24Vision:*`
Expected: на превью видны рамки, сверху CPU% и GPU% (или `n/a`). Зафиксировать FPS в логе.

- [ ] **Step 4: Commit**

```bash
git add s24/app/src/main/java/com/s24vision/app/ui/Nav.kt s24/app/src/main/java/com/s24vision/app/ui/CameraScreenState.kt s24/app/src/main/java/com/s24vision/app/camera/ImageProxyExt.kt
git commit -m "feat(s24): wire live pipeline, monitoring overlay, navigation"
```

### Task 16: Экраны Записи, Обучение, Профили

**Files:**
- Create: `s24/app/src/main/java/com/s24vision/app/ui/RecordingsScreen.kt`
- Create: `s24/app/src/main/java/com/s24vision/app/ui/TrainScreen.kt`
- Create: `s24/app/src/main/java/com/s24vision/app/ui/ProfilesScreen.kt`

- [ ] **Step 1: RecordingsScreen.kt — список mp4 + кнопка «Обучить по ролику»**

```kotlin
package com.s24vision.app.ui
import androidx.compose.foundation.layout.*
import androidx.compose.foundation.lazy.LazyColumn
import androidx.compose.foundation.lazy.items
import androidx.compose.material3.*
import androidx.compose.runtime.*
import androidx.navigation.NavController

@Composable
fun RecordingsScreen(nav: NavController) {
    val files = remember { AppDeps.recordingsDir().listFiles()?.toList()?.sortedDescending() ?: emptyList() }
    LazyColumn(Modifier.fillMaxSize().padding(12.dp)) {
        items(files) { f ->
            ListItem(
                headlineContent = { Text(f.name) },
                trailingContent = {
                    Button(onClick = { nav.navigate("train?video=${f.path}") }) { Text("Обучить") }
                }
            )
        }
    }
}
```

- [ ] **Step 2: TrainScreen.kt — имя + «Это человек?» + результат с сообщениями**

```kotlin
package com.s24vision.app.ui
import androidx.compose.foundation.layout.*
import androidx.compose.material3.*
import androidx.compose.runtime.*
import com.s24vision.app.core.profiles.EnrollResult

@Composable
fun TrainScreen(videoPath: String? = null) {
    var name by remember { mutableStateOf("") }
    var isPerson by remember { mutableStateOf(true) }
    var message by remember { mutableStateOf("") }
    Column(Modifier.fillMaxSize().padding(16.dp)) {
        OutlinedTextField(value = name, onValueChange = { name = it }, label = { Text("Имя") })
        Row(verticalAlignment = androidx.compose.ui.Alignment.CenterVertically) {
            Text("Это человек?"); Switch(checked = isPerson, onCheckedChange = { isPerson = it })
        }
        Button(enabled = name.isNotBlank() && videoPath != null, onClick = {
            val existed = AppDeps.profileExists(name, isPerson)
            val outcome = AppDeps.enroller().enroll(videoPath!!, name, isPerson)
            message = renderMessage(existed, outcome)
        }) { Text("Обучить / Дообучить") }
        Spacer(Modifier.height(12.dp))
        Text(message)
    }
}

private fun renderMessage(existedBefore: Boolean, o: com.s24vision.app.enroll.Enroller.Outcome): String {
    val r = o.face ?: o.body ?: o.obj ?: return "Объект в ролике не найден"
    return when (r) {
        is EnrollResult.Created -> "Профиля не было — создан новый (${r.total} образцов)"
        is EnrollResult.Improved -> "Профиль улучшен: было ${r.before}, стало ${r.after}"
        is EnrollResult.Empty -> "В ролике не найдено подходящих кадров"
    }
}
```

> Сообщения соответствуют требованию: нет профиля → «создан новый»; есть → «улучшен: было N, стало M».

- [ ] **Step 3: ProfilesScreen.kt — список + удаление**

```kotlin
package com.s24vision.app.ui
import androidx.compose.foundation.layout.*
import androidx.compose.foundation.lazy.LazyColumn
import androidx.compose.foundation.lazy.items
import androidx.compose.material3.*
import androidx.compose.runtime.*
import com.s24vision.app.core.profiles.ProfileType

@Composable
fun ProfilesScreen() {
    val store = AppDeps.store()
    var refresh by remember { mutableStateOf(0) }
    val rows = remember(refresh) {
        ProfileType.values().flatMap { t -> store.list(t).map { t to it } }
    }
    LazyColumn(Modifier.fillMaxSize().padding(12.dp)) {
        items(rows) { (t, name) ->
            ListItem(
                headlineContent = { Text("$name") },
                supportingContent = { Text(t.name) },
                trailingContent = { Button(onClick = { store.delete(t, name); refresh++ }) { Text("Удалить") } }
            )
        }
    }
}
```

> `AppDeps` — простой объект-холдер зависимостей (store, enroller, recordingsDir, profileExists), инициализируется в `MainActivity`. Создать `ui/AppDeps.kt` с этими функциями, используя `filesDir` приложения как root для `ProfileStore` и `recordings/`.

- [ ] **Step 4: AppDeps.kt**

```kotlin
package com.s24vision.app.ui
import android.content.Context
import com.s24vision.app.core.profiles.ProfileStore
import com.s24vision.app.core.profiles.ProfileType
import com.s24vision.app.enroll.Enroller
import java.io.File

object AppDeps {
    private lateinit var appCtx: Context
    private lateinit var storeRef: ProfileStore
    private lateinit var enrollerRef: Enroller
    fun init(ctx: Context, enroller: Enroller) {
        appCtx = ctx.applicationContext
        storeRef = ProfileStore(appCtx.filesDir)
        enrollerRef = enroller
    }
    fun store() = storeRef
    fun enroller() = enrollerRef
    fun recordingsDir() = File(appCtx.filesDir, "recordings")
    fun profileExists(name: String, isPerson: Boolean) =
        if (isPerson) storeRef.exists(ProfileType.FACE, name) || storeRef.exists(ProfileType.BODY, name)
        else storeRef.exists(ProfileType.OBJECT, name)
}
```

- [ ] **Step 5: Сборка + устройство (обучение по ролику end-to-end)**

Run: `./gradlew :app:installDebug`
Expected: записать ролик → открыть «Записи» → «Обучить» → ввести имя, выбрать «человек/объект» → увидеть сообщение «создан/улучшен». Проверить, что в новом сеансе камера подписывает обученное имя.

- [ ] **Step 6: Commit**

```bash
git add s24/app/src/main/java/com/s24vision/app/ui/RecordingsScreen.kt s24/app/src/main/java/com/s24vision/app/ui/TrainScreen.kt s24/app/src/main/java/com/s24vision/app/ui/ProfilesScreen.kt s24/app/src/main/java/com/s24vision/app/ui/AppDeps.kt
git commit -m "feat(s24): add recordings, train, profiles screens"
```

---

## Phase 5 — Документация установки

### Task 17: README с инструкцией установки на S24 (Mac M1)

**Files:**
- Create: `s24/README.md`

- [ ] **Step 1: Написать README**

```markdown
# S24 Vision

Android-приложение для Samsung S24: распознавание объектов (YOLOE-26), запись видео
с рамками, обучение/дообучение людей (лицо+тело) и объектов по роликам, мониторинг CPU/GPU.

## 1. Подготовка моделей (один раз, на Mac M1)
cd hikvision-test1 && source .venv/bin/activate && pip install -U ultralytics
python ../s24/tools/export_models.py
# модели появятся в s24/app/src/main/assets/models/

## 2. Сборка APK (Mac M1)
Установить Android Studio (Apple Silicon) + Android SDK 35.
cd s24 && ./gradlew :app:assembleDebug
# APK: app/build/outputs/apk/debug/app-debug.apk

## 3. Установка на телефон
1. На S24: Настройки → «О телефоне» → 7 раз нажать «Номер сборки» → включится режим разработчика.
2. Настройки → «Параметры разработчика» → включить «Отладка по USB».
3. Подключить S24 к Mac кабелем, разрешить отладку на телефоне.
4. adb devices  (должен появиться серийник)
5. cd s24 && ./gradlew :app:installDebug
   (или: adb install -r app/build/outputs/apk/debug/app-debug.apk)
6. Запустить «S24 Vision», разрешить доступ к камере.

## 4. Использование
- Камера: переключение селфи/основная, рамки и имена в реальном времени, CPU%/GPU%, REC.
- Записи: ролики из памяти телефона, кнопка «Обучить по ролику».
- Обучение: имя + «Это человек?» → «Обучить/Дообучить». Сообщения «создан»/«улучшен».
- Профили: список и удаление.

## 5. Где файлы на телефоне
Android/data/com.s24vision.app/files/{recordings, profiles/{faces,bodies,objects}}

## 6. Отладка
adb logcat -s S24Vision:* *:E
```

- [ ] **Step 2: Commit**

```bash
git add s24/README.md
git commit -m "docs(s24): add build and install guide for Samsung S24"
```

---

## Self-Review

**1. Spec coverage:**
- GUI + обе камеры → Task 12 (CameraController toggleLens, CameraScreen). ✓
- Распознавание объектов YOLOE → Task 9, 11. ✓
- Запись видео с рамками в папку → Task 13 + Task 15 (подключение recorder). ✓
- Обучение человек(лицо+тело)/объект с вопросом «человек?» → Task 14, 16 (TrainScreen switch). ✓
- Дообучение + сообщения «создан/улучшен/нет» → Task 4 (EnrollResult), Task 16 (renderMessage). ✓
- Выбор ролика из папки и дообучение → Task 16 (RecordingsScreen → TrainScreen). ✓
- Нагрузка CPU/GPU % → Task 5, 6, 15 (statsLabel). ✓
- Установка на S24 с Mac M1 → Task 17. ✓
- Размер модели «не более среднего» → YOLOE `s`, остальные лёгкие (Task 1). ✓

**2. Placeholder scan:** Осознанные TODO привязаны к артефактам, доступным только после экспорта моделей и проверки на устройстве: `FaceRecognizer.detectAndAlign` (форма SCRFD), `YoloeDetector.decode`/`CocoLabels` (форма выхода YOLOE), `FrameRecorder.encode` (дренаж MediaCodec), YUV→Bitmap extension. Каждый имеет временную рабочую реализацию или чётко описанную процедуру; финализация — на шагах «устройство».

**3. Type consistency:** `EnrollResult` (Created/Improved/Empty) — Task 4 и используется в Task 16. `Detection`/`Annotation` — Task 9/11 и UI. `ProfileType` (FACE/BODY/OBJECT) — Task 4, 11, 14, 16. `Embeddings.bestMatch` сигнатура — Task 3, используется в 10/11. `CameraScreenState` поля совпадают между Task 12 (использование) и Task 15 (определение). `AppDeps` методы — Task 16. Согласовано.

**Известные зависимости от железа/моделей (вынесены в шаги «устройство»):** точные имена входов/выходов ONNX, формы тензоров YOLOE/SCRFD, полный словарь меток prompt-free, доступность Adreno sysfs для GPU%. Это не плейсхолдеры логики, а калибровка под реальные артефакты, которую нельзя зафиксировать до экспорта.
